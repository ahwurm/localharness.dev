---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<div class:list={['border border-edge bg-surface/60', !/\bp-\d/.test(className) && 'p-6', className]}>
  <slot />
</div>
